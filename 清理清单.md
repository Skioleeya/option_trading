# Python 清理清单

> 生成日期：2026-04-02  
> 依据：全库 numpy/scipy 扫描 + Rust 迁移现状核查  
> 原则：只清理有实际收益的目标；不动合理保留的 GPU/Numba 路径

---

## 一、新增 Rust 迁移目标（有实质计算收益）

### P1 — `l2_decision/fusion/attention_fusion.py`（203 行）⭐ 最高优先

**现状：** L2 决策热路径，每 tick 调用，使用 numpy softmax + `np.dot` 做信号加权融合。是目前**唯一一个从未被 Rust 触碰、且有实质 numpy 运算的文件**。

**目标：**
- 新建 `shared_rust_services/src/fusion.rs`
  - 实现 `compute_attention_fused(signal_values, logits, regime_key) -> (f64, f64)` PyO3 函数
  - 内置数值稳定 softmax（与 Python `_softmax` 等价）
  - 返回 `(raw_score, calibrated_confidence)`
- 编辑 `l2_decision/fusion/attention_fusion.py`：委托 Rust，移除 numpy 依赖
- 注册到 `shared_rust_services/src/lib.rs`，重编译 pyd

**接受标准：**
- `python -c "from shared_rust.services import compute_attention_fused; print('fusion-ok')"` 通过
- 与 Python 实现相对误差 < 1e-10（数值稳定 softmax 验证）
- `pwsh scripts/validate_session.ps1 -Strict` 通过

**OpenSpec 提案：** 待创建 `impl-20260402-l2-attention-fusion-rust`

---

### P2 — `l1_compute/microstructure/wall_context_builder.py`（130 行）

**现状：** 4 处 numpy 调用，属于 L1 微结构路径。`microstructure.rs` 已存在（VPIN/VolAccel/Entropy），只需**追加函数**，无需新建模块。

**目标：**
- 追加到 `shared_rust_services/src/microstructure.rs`（当前已有 3 个函数，加新函数后仍 ≤ 400 行）
- 编辑 `wall_context_builder.py`：移除 numpy，委托 Rust
- 重编译 pyd

**接受标准：**
- 微结构路径 smoke 通过
- `microstructure.rs` 保持 ≤ 400 行，无 `unwrap()`

**OpenSpec 提案：** 待创建 `impl-20260402-l1-wall-context-rust`

---

## 二、残留 import 清理（无需 Rust 开发，1–2 行修改）

### C1 — `l1_compute/aggregation/streaming_aggregator.py`

**问题：** Rust 迁移已完成，但仍有 `import numpy as np`。  
**用途核查：** numpy 仅用于 `np.asarray()` 在调用 Rust 前做类型转换（PyO3 桥接）。  
**修复方案：** 确认 PyO3 函数是否接受 Python list 直接传入；若接受则删除 `np.asarray` 包装 + `import numpy`；若不接受则保留并标注 `# PyO3 bridge: array marshalling`。  
**工作量：** 极小（核查 + 1 行修改或 1 行注释）

### C2 — `l1_compute/analysis/greeks_engine.py`

**问题：** numpy 用于构建传给 BSM compute 的数组（`np.full`、`np.zeros` 等）。  
**用途核查：** 这些数组随后传入 `compute_greeks_batch`（路由到 CuPy/Numba/Rust）。  
**修复方案：** 若 PyO3 接受 list，替换为 Python list 推导式；若不接受，标注保留原因。  
**工作量：** 极小

---

## 三、OpenSpec 收口任务（无代码改动）

### O1 — `refactor-dependency-20260402-shared-rust-services-namespace-closeout`
- **任务：** 执行 4 条 grep 扫描（0 命中验证）+ 关闭 backlog 项
- **阻塞：** 无，可立即执行
- **文件：** `openspec/changes/refactor-dependency-20260402-shared-rust-services-namespace-closeout/tasks.md`

### O2 — `impl-20260402-l0-runtime-neutral-surface-closeout`
- **Step 1（可立即）：** `pwsh scripts/test/run_pytest.ps1 tests/l0_runtime/`（ACL 已修复）
- **Step 2（等实盘）：** Sub-wave F dual-run 证据收集（下一个交易时段）
- **文件：** `openspec/changes/impl-20260402-l0-runtime-neutral-surface-closeout/tasks.md`

---

## 四、明确不动的文件（避免误操作）

| 文件 | 原因 |
|------|------|
| `l1_compute/analysis/bsm_fast.py` | CuPy(Tier1)/Numba(Tier2) 是合法性能路径；Tier3 已是 Rust |
| `l1_compute/compute/gpu_greeks_kernel.py` | GPU 核心，Rust 无法替代 CuPy |
| `l1_compute/analysis/atm_decay/`（1,558 行） | 零 numpy，纯时序状态机，无迁移价值 |
| `l1_compute/trackers/wall_migration_tracker.py`（362 行） | 零 numpy，使用 Rust 数据结构，状态逻辑保留 Python |
| `l1_compute/reactor.py` | numpy 仅做数组分配（`np.full/zeros`），是传递给 Rust 的前置步骤 |
| `shared/services/l0_runtime/`（28 文件） | 非 shim 协调器，需独立提案才能退役 |
| `l3_assembly/`、`app/` | I/O 绑定 / UI 组装层，无计算迁移价值 |

---

## 五、执行顺序建议

```
立即可做（不需要实盘）
  O1  namespace 收口 grep 扫描
  O2-Step1  l0_runtime 测试套件
  C1  streaming_aggregator numpy 核查
  C2  greeks_engine numpy 核查

下一个 Codex 任务批次
  P1  attention_fusion.py → fusion.rs   ← 最高 ROI
  P2  wall_context_builder.py → microstructure.rs 追加

等待实盘交易时段
  O2-Step2  Sub-wave F dual-run 证据
```

---

## 六、完成度指标（当前基线 2026-04-02）

| 层 | 热路径 Rust 覆盖率 | 残留 numpy 文件数 | 目标 |
|----|-------------------|------------------|------|
| L1 compute | ~85% | 7（含合理保留） | P1/P2 后 → 5 |
| L2 decision | ~70% | 1（attention_fusion） | P1 后 → 0 |
| L0 ingest | ~95% | 0 | 保持 |
| L3 assembly | ~60% | 0 | 保持（I/O 层） |
