# 最新启动步骤文档（Windows 正式运行 Owner）

## 1. 适用范围

本流程用于在 Windows 主机上启动 `E:\US.market\Option_v4` 的正式运行栈：

1. Redis（6380）
2. Backend（8001）
3. Frontend（5173）

正式运行目录固定为：`E:\US.market\Option_v4`

## 2. 启动前检查

1. 进入仓库根目录：

```powershell
Set-Location E:\US.market\Option_v4
```

2. 只允许使用仓库本地 Python 入口：

```powershell
.\.venv\Scripts\python.exe manage.py --help
```

3. Probe-first（必须）：

```powershell
.\.venv\Scripts\python.exe manage.py start-all --verify-only
```

4. Redis owner 合同：

- Redis 默认二进制路径固定为 `infra\bin\redis-server.exe`
- Redis 数据目录固定为仓库内 `var\redis`
- 若仓库固定路径不存在，只允许临时使用 `--redis-exe <abs-path>` 诊断；正式交付必须恢复为仓库固定路径

5. 前端合同：

- 浏览器只访问 `http://localhost:5173`
- API 和 WebSocket 只走同源 `/api` 与 `/ws`
- 不允许浏览器直连 `127.0.0.1:8001`

## 3. 标准启动

```powershell
.\.venv\Scripts\python.exe manage.py start-all
```

说明：

1. 固定顺序：`Redis -> Backend(strict) -> Frontend`
2. 前端由 `start-all` 自动注入 `VITE_BACKEND_ORIGIN`
3. Backend 启动必须通过 LongPort strict connectivity probe
4. 命令结束时会输出 `6380/8001/5173` 监听核验表

## 4. 诊断命令

```powershell
# 仅核验，不启动
.\.venv\Scripts\python.exe manage.py start-all --verify-only

# 后端 dry-run
.\.venv\Scripts\python.exe manage.py start-backend --dry-run

# 边界扫描
.\.venv\Scripts\python.exe manage.py check-layer-boundaries

# 严格校验
.\.venv\Scripts\python.exe manage.py validate-session --strict
```

## 5. 常见故障

1. `Redis executable not found`
   - 检查 `infra\bin\redis-server.exe` 是否存在
   - 若临时诊断，可传 `--redis-exe <abs-path>`

2. 前端未监听 `5173`
   - 先确认 `l4_ui\node_modules` 已就位
   - 再检查 `logs\frontend_runtime.current.log`

3. Backend strict 启动失败
   - 优先检查 LongPort 凭据是否已在 User/Machine 环境变量设置
   - 查看 `logs\backend_runtime.current.log`

4. `build-pyd` 失败且报 `SEC_E_NO_CREDENTIALS`
   - 这是主机 TLS/证书链问题，先修复 Windows schannel 凭据链，再重跑 `build-pyd`

## 6. 完成标准

以下条件同时满足，才算 Windows 环境落地完成：

1. `.\.venv\Scripts\python.exe manage.py start-all` 成功
2. `.\.venv\Scripts\python.exe manage.py start-all --verify-only` 显示三项服务全为 `True`
3. 浏览器可访问 `http://localhost:5173`
4. `.\.venv\Scripts\python.exe manage.py validate-session --strict` 通过
