# 最新启动步骤文档（外部提权）

## 1. 适用范围

本流程用于在 **宿主机外部提权** 环境启动以下服务：

1. Redis（6380）
2. Backend（8001）
3. Frontend（5173）

仓库根目录：`E:\US.market\Option_v3`

## 2. 启动前检查

1. 用管理员权限打开 PowerShell（外部提权）。
2. 进入仓库根目录：

```powershell
Set-Location E:\US.market\Option_v3
```

3. 检查端口占用（可选）：

```powershell
Get-NetTCPConnection -State Listen -ErrorAction SilentlyContinue |
  Where-Object { $_.LocalPort -in 6380,8001,5173 } |
  Select-Object LocalAddress,LocalPort,OwningProcess |
  Sort-Object LocalPort
```

## 3. 推荐：一键启动 + 一键核验（start_all.ps1）

优先使用：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\ops\start_all.ps1
```

说明：

1. 固定顺序：`Redis -> Backend(strict) -> Frontend`
2. 若后端 strict 超时未就绪，脚本会自动尝试 `-Degraded` 重启后端（除非显式禁用）
3. 脚本结束时自动输出 6380/8001/5173 监听核验表

常用参数：

```powershell
# 只做核验（不启动）
powershell -ExecutionPolicy Bypass -File scripts\ops\start_all.ps1 -VerifyOnly

# 禁用后端 degraded 自动重试
powershell -ExecutionPolicy Bypass -File scripts\ops\start_all.ps1 -NoDegradedRetry
```

## 4. 手动启动顺序（回退方案，必须按顺序）

1. 启动 Redis：

```powershell
cmd /c "scripts\infra\redis-start.bat"
```

2. 启动 Backend（严格模式，默认）：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\ops\start_backend.ps1
```

3. 仅在严格模式因 Broker 连通性失败时，才允许降级重启 Backend：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\ops\start_backend.ps1 -Degraded
```

4. 启动 Frontend（Vite）：

```powershell
cmd /c "cd /d E:\US.market\Option_v3\l4_ui && npm run dev -- --host 0.0.0.0 --port 5173 >> E:\US.market\Option_v3\logs\frontend_runtime.current.log 2>&1"
```

## 5. 启动后核验（必须执行）

1. 检查三端口监听：

```powershell
Get-NetTCPConnection -State Listen -ErrorAction SilentlyContinue |
  Where-Object { $_.LocalPort -in 6380,8001,5173 } |
  Select-Object LocalAddress,LocalPort,OwningProcess |
  Sort-Object LocalPort
```

期望结果：

1. `6380`（Redis）监听
2. `8001`（Backend）监听
3. `5173`（Frontend）监听

2. 检查关键日志：

```powershell
Get-Content logs\backend_runtime.current.log -Tail 50
Get-Content logs\frontend_runtime.current.log -Tail 50
```

## 6. 常用访问地址

1. Frontend: `http://localhost:5173`
2. Backend: `http://localhost:8001`

## 7. 故障处理要点

1. 若 `8001` 未监听，先看 `logs\backend_runtime.current.log`，确认是否是 strict 连通性失败。
2. strict 连通性失败时，按第 3.3 步使用 `-Degraded` 启动。
3. 若 `5173` 未监听，确认 `l4_ui` 目录存在 `node_modules`，并检查前端日志。
4. 若端口被占用，先停止冲突进程后重启对应服务。
