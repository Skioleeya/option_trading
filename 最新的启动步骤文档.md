# 最新的启动步骤文档

## 1. 文档目标

这份文档只描述当前仓库已经实现并在 Windows 主机上验证过的标准启动合同。

适用目录固定为：

```powershell
E:\US.market\Option_v4
```

标准服务与端口固定为：

1. Redis：`6380`
2. Backend：`8001`
3. Frontend：`5173`

标准入口固定为：

```powershell
.\.venv\Scripts\python.exe manage.py start-all
```

除这条入口外，直接 `uvicorn`、直接 `npm run dev`、直接浏览器连 `8001` 都不算正式启动证据。

盘前自动拉起入口固定为：

```powershell
# 生成计划任务预览
.\.venv\Scripts\python.exe manage.py register-start-all-task --output-dir tmp/schtasks

# 安装/更新计划任务
.\.venv\Scripts\python.exe manage.py register-start-all-task --apply
```

自动拉起合同：

1. Windows Task Scheduler 固定按 `MON-FRI 09:25` 触发
2. 仓库命令 `run-scheduled-start-all` 会先执行标准 `start-all`
3. `start-all` 完成后，命令会再次用 `XNYS` 交易日历判断当天是否为美股交易日
4. 若当天不是美股交易日，则计划任务会显式关闭刚拉起的 Redis、Backend、Frontend 栈

## 2. 当前真实启动语义

`start-all` 的当前实现语义是：

1. 只允许在 Windows 主机运行
2. 启动顺序固定为 `Redis -> Backend(strict-only) -> Frontend`
3. Redis 读取 `infra\redis\redis.conf.local`
4. Redis 默认二进制固定为 `infra\bin\redis-server.exe`
5. Redis 数据目录固定为仓库内 `var\redis`
6. Redis 启动前会做 preflight：
   - 目录必须可用
   - 文件系统必须满足仓库合同
   - AOF 总量必须不超过 `2 GiB`
7. Backend 启动固定走 strict 模式，不允许 degraded
8. Backend 启动前会尝试优雅停止已有 `uvicorn main:app` 进程；超时后仍残留则拒绝继续替换
9. Frontend 当前标准启动面不是 dev server，而是：

```text
node ./scripts/preview-strict.mjs --host 0.0.0.0 --port 5173 --strictPort
```

10. Frontend 若发现 `5173` 已被占用，会先按 Vite 进程清理；如果还占用，再按监听该端口的 PID 强制清理
11. Frontend 的就绪条件不是“端口打开”而是“HTTP 可访问”
12. `start-all --verify-only` 的当前实现只检查三项端口是否在监听，不会额外访问 backend `/health`

## 3. 浏览器与代理合同

当前前端/浏览器合同固定为：

1. 浏览器只访问 `http://localhost:5173`
2. 浏览器侧 API 只走同源 `/api`
3. 浏览器侧 WebSocket 只走同源 `/ws`
4. 浏览器不允许直连 `http://127.0.0.1:8001`
5. `VITE_BACKEND_ORIGIN` 只属于 Vite 代理/启动层，不再是浏览器 bundle 的硬依赖
6. 浏览器运行时如果已经有当前页面 origin，就以当前 frontend origin 作为 same-origin 基准，不因为 bundle 内没有 `VITE_BACKEND_ORIGIN` 而崩溃

## 4. 启动前准备

先进入仓库根目录：

```powershell
Set-Location E:\US.market\Option_v4
```

确认使用仓库本地 Python：

```powershell
.\.venv\Scripts\python.exe manage.py --help
```

如果前端源码、`vite.config.mjs`、`l4_ui/scripts/*` 或前端运行时配置有变化，先重建前端产物：

```powershell
npm --prefix l4_ui run build
```

原因很直接：当前标准 frontend 面是 `preview-strict`，它使用 `l4_ui/dist`，不是 `vite dev` 的即时编译结果。

## 5. 标准启动步骤

执行：

```powershell
.\.venv\Scripts\python.exe manage.py start-all
```

当前这条命令的预期过程是：

1. 输出 `RepoRoot`
2. 输出固定启动顺序和 readiness timeout
3. Redis preflight 通过
4. Redis 若已监听则复用；否则拉起并等待 `PING=PONG`
5. Backend strict 启动，等待 `/health=200`
6. Frontend 通过 `preview-strict` 拉起，并等待 `http://127.0.0.1:5173` 真正可访问
7. 最后输出三项 `Verification summary`

成功结束时会打印：

```text
[start-all] All services are up.
[start-all] Frontend: http://localhost:5173
[start-all] Backend:  http://localhost:8001
```

## 6. 启动后的标准复核

先跑端口级复核：

```powershell
.\.venv\Scripts\python.exe manage.py start-all --verify-only
```

当前实现下，这条命令只检查以下三项是否在监听：

```text
Redis      6380
Backend    8001
Frontend   5173
```

要补足“服务真的可用”，还要再跑两条主机探针：

```powershell
Invoke-WebRequest -UseBasicParsing http://127.0.0.1:8001/health
Invoke-WebRequest -UseBasicParsing http://127.0.0.1:5173
```

当前正确结果应该满足：

1. `/health` 返回 `200`
2. `/health` 响应 JSON 中 `status` 为 `ok`
3. `/health` 响应 JSON 中 `research_persistence.healthy` 为 `true`
4. `http://127.0.0.1:5173` 返回 `200`

## 7. 当前标准日志位置

如果启动失败，优先看这些日志：

1. Redis：`logs\redis_runtime.current.log`
2. Backend：`logs\backend_runtime.current.log`
3. Frontend：`logs\frontend_runtime.current.log`

## 8. 诊断命令

```powershell
# 只做端口监听核验，不重启
.\.venv\Scripts\python.exe manage.py start-all --verify-only

# 查看 backend 启动参数，不真正启动
.\.venv\Scripts\python.exe manage.py start-backend --dry-run

# 重建当前 frontend 产物
npm --prefix l4_ui run build

# 会话严格校验
.\.venv\Scripts\python.exe manage.py validate-session --strict
```

## 9. 常见故障与当前处理方式

### 9.1 `Redis executable not found`

含义：仓库固定 Redis 二进制不存在。  
当前默认路径就是：

```text
infra\bin\redis-server.exe
```

### 9.2 `Frontend port 5173 remains occupied after Vite cleanup`

含义：`start-all` 先按 Vite 进程清理，之后连端口级兜底清理也没能释放 `5173`。  
处理顺序：

1. 确认没有其他进程长期占用 `5173`
2. 看 `logs\frontend_runtime.current.log`
3. 再重新执行 `start-all`

### 9.3 浏览器报 `VITE_BACKEND_ORIGIN is required`

这不应再发生在当前合同下。  
如果还出现，几乎总是旧 bundle 还在被服务：

1. 先执行：

```powershell
npm --prefix l4_ui run build
```

2. 然后重新执行：

```powershell
.\.venv\Scripts\python.exe manage.py start-all
```

### 9.4 Backend strict 启动失败

优先检查：

1. LongPort 凭据是否有效
2. `logs\backend_runtime.current.log`
3. `/health` 是否真的能到 `200`

### 9.5 Frontend 已拉起但页面空白或旧代码

优先检查：

1. `npm --prefix l4_ui run build` 是否已重跑
2. `logs\frontend_runtime.current.log`
3. 页面实际加载的 JS 文件名是否对应最新 `dist/assets/index-*.js`

## 10. 本文档与当前实现的边界

本文档只同步以下当前实现：

1. `infra/ops_cli/start_all.py`
2. `infra/ops_cli/start_backend.py`
3. `l4_ui/scripts/preview-strict.mjs`
4. `l4_ui/vite.config.mjs`
5. `l4_ui/src/config/runtime.ts`
6. `docs/SOP/L4_FRONTEND.md`

如果这些实现再变，这份文档也必须同步更新；否则视为文档失真。
