name: Session Validation

on:
  pull_request:
  workflow_dispatch:

jobs:
  validate-session:
    runs-on: windows-latest
    steps:
      - name: Intentional fail for required-check block test
        shell: pwsh
        run: |
          Write-Host "intentional failure for gate verification"
          exit 1
