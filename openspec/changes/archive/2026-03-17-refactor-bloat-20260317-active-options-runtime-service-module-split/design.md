## Design Summary

采用“先提纯职责、再稳定接口”的拆分策略：

1. 提炼纯函数辅助模块，避免 service 文件承载实现细节。
2. 保留 orchestrator 风格入口，减少模块互相反向依赖。
3. 通过回归测试验证行为无漂移。

## Constraints

- 拆分后依赖方向单向。
- 不引入循环依赖。
- 不引入复杂函数与深层嵌套。
- 禁止无语义中间层与重复函数。

## Validation Plan

1. 模块边界扫描通过。
2. 相关 pytest 回归通过。
3. 严格门禁通过并记录。
