# Option_v3 启动步骤（实测可用版）

本文档按 2026-03-17 实测成功流程编写，目标是一次性拉起：
- Redis（6380）
- 后端 FastAPI/Uvicorn（8001）
- 前端 Vite（5173）

---

## 1. 先探测（probe-first）

在 `PowerShell` 执行：

```powershell
Set-Location E:\US.market\Option_v3

try { (Invoke-WebRequest http://127.0.0.1:8001/health -UseBasicParsing -TimeoutSec 3).StatusCode } catch { "BACKEND_DOWN" }
try { (Invoke-WebRequest http://127.0.0.1:5173 -UseBasicParsing -TimeoutSec 3).StatusCode } catch { "FRONTEND_DOWN" }
cmd /c netstat -ano | findstr ":6380 .*LISTENING"
```

判定：
- `/health = 200` 说明后端已运行，勿重复启动。
- `5173 = 200` 说明前端已运行，勿重复启动。
- `6380 LISTENING` 说明 Redis 已运行。

---

## 2. 按需启动 Redis

> 仅当 6380 未监听时执行。

```powershell
Set-Location E:\US.market\Option_v3
$redisBin  = "E:\US.market\Option_v3\infra\bin\redis-server.exe"
$redisConf = "E:\US.market\Option_v3\infra\redis\redis.conf.local"

Start-Process -FilePath $redisBin -ArgumentList "`"$redisConf`"" -WorkingDirectory "E:\US.market\Option_v3"
```

验证：

```powershell
cmd /c netstat -ano | findstr ":6380 .*LISTENING"
```

---

## 3. 启动后端（标准方法）

### 3.1 默认后台启动（推荐）

```powershell
Set-Location E:\US.market\Option_v3
.\scripts\ops\start_backend.ps1
Start-Sleep -Seconds 8
try { (Invoke-WebRequest http://127.0.0.1:8001/health -UseBasicParsing -TimeoutSec 4).StatusCode } catch { "BACKEND_DOWN" }
```

说明：
- 默认是后台模式，终端不会持续刷日志，这是预期行为。
- stdout/stderr 会追加到 `logs/backend_runtime.current.log`。
- 实时看日志请执行：

```powershell
Get-Content logs\backend_runtime.current.log -Wait -Tail 120
```

### 3.2 strict 失败时回退 degraded

如果 3.1 不是 `200`，执行：

```powershell
Set-Location E:\US.market\Option_v3
.\scripts\ops\start_backend.ps1 -Degraded
Start-Sleep -Seconds 12
try { (Invoke-WebRequest http://127.0.0.1:8001/health -UseBasicParsing -TimeoutSec 4).StatusCode } catch { "BACKEND_DOWN" }
```

### 3.3 前台调试启动（需要直接看控制台输出时）

```powershell
Set-Location E:\US.market\Option_v3
.\scripts\ops\start_backend.ps1 -Foreground
```

说明：
- 前台模式会把日志同时输出到控制台并追加到 `logs/backend_runtime.current.log`。
- 适用于排查“启动无输出/初始化失败”类问题。

---

## 4. 启动前端

> 仅当 5173 不可用时执行。

```powershell
Set-Location E:\US.market\Option_v3
Start-Process -FilePath "cmd.exe" -ArgumentList "/c","npm --prefix l4_ui run dev -- --host 0.0.0.0 --port 5173" -WorkingDirectory "E:\US.market\Option_v3"
Start-Sleep -Seconds 10
try { (Invoke-WebRequest http://127.0.0.1:5173 -UseBasicParsing -TimeoutSec 4).StatusCode } catch { "FRONTEND_DOWN" }
```

---

## 5. 一次性验活（最终检查）

```powershell
try { (Invoke-WebRequest http://127.0.0.1:8001/health -UseBasicParsing -TimeoutSec 3).StatusCode } catch { "BACKEND_DOWN" }
try { (Invoke-WebRequest http://127.0.0.1:5173 -UseBasicParsing -TimeoutSec 3).StatusCode } catch { "FRONTEND_DOWN" }
cmd /c netstat -ano | findstr ":6380 .*LISTENING"
```

成功标准：
- 后端：`200`
- 前端：`200`
- Redis：`6380 LISTENING`

---

## 6. 常见问题

- 后端 strict 起不来：按第 3.2 节 degraded 启动。
- 启动后“终端无输出”：这是后台模式默认行为；用 `Get-Content logs/backend_runtime.current.log -Wait -Tail 120` 追踪日志。
- 需要实时控制台日志：使用第 3.3 节 `-Foreground` 启动。
- 端口冲突：先 probe，确认是否已有健康实例，避免重复起服务。
- 前端首次慢启动：延长 `Start-Sleep` 到 15~20 秒再验活。
