# L0-L4 架构审计清单

## P0（必须先完成）
- [x] 去除 `l2_decision -> l3_assembly` 直接依赖（`AgentG` 不再直接 import L3 Presenter）。
- [x] 去除 `l3_assembly -> l2_decision` 直接依赖（ActiveOptions 计算从 L3 UI Presenter 解耦到中立服务层）。
- [x] 移除编排层对私有成员访问（`_active_options_presenter`），改为公开接口或独立服务注入。

## P1（短期完成）
- [ ] 收敛 `l3_assembly/assembly/ui_state_tracker.py` 对 `l1_compute` tracker/analysis 的直接依赖，改为消费 L1 输出契约。
- [ ] 收敛 `l2_decision/agents/services/gamma_analyzer.py` 对 `l1_compute.analysis` 的直接调用，改为 shared contract/service。
- [ ] 为 L0-L4 建立“禁止反向导入”校验（CI 中做 import boundary 检查）。

## P2（增强项）
- [ ] 收紧 L4 类型契约，减少 `any/unknown`（`tactical_triad/skew_dynamics/mtf_flow/active_options`）。
- [ ] 增加跨层契约回归测试（L1 输出字段 -> L2 决策 -> L3 payload -> L4 渲染一致性）。
- [ ] 整理并逐步移除 legacy shim（如 `to_legacy_agent_result`）并替换为 typed contract 直通。

## 验收标准
- [x] L0/L1/L2/L3/L4 仅单向依赖，不存在反向 import。
- [x] 编排层仅依赖公开接口，不访问私有实现细节。
- [ ] L4 消费字段均有稳定类型约束与归一化模型。
- [ ] 关键链路通过端到端回归（含 `test_l0_l4_pipeline.py`）。
